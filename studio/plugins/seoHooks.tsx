/**
 * Automated SEO hooks for Sanity Studio
 * Automatically calculates reading time, word count, and validates SEO on document save
 */

import {definePlugin} from 'sanity'

// Simple SEO hooks plugin
export const seoHooksPlugin = definePlugin({
  name: 'seo-hooks-plugin',
  plugins: [],
  // Plugin functionality will be added later
})

export default seoHooksPlugin